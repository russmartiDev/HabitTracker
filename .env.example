# Auth.js v5 — required. Generate with: openssl rand -base64 32
AUTH_SECRET=

# Anthropic — optional. App falls back to algorithmic grading if absent.
ANTHROPIC_API_KEY=

# Auth.js base URL (defaults to http://localhost:3000 in dev)
# AUTH_URL=http://localhost:3000
