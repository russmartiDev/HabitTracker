## 1. Implementation

- [x] 1.1 In `CheckInForm.tsx` `MoodStep`, replace the two sibling `row between` containers (one for buttons, one for labels) with a single `row between` where each child is a `flex-column` wrapping one button + its `MOOD_LABELS[i]` span

## 2. Verification

- [x] 2.1 Check-in mood step — each label is visually centered directly under its emoji button
- [x] 2.2 Resize the browser window — alignment holds at all widths
